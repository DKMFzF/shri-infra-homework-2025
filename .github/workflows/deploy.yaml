name: Deploy Project

on:
  workflow_dispatch:
    inputs:
      RV:
        description: "Версия релиза (например, 1.0.0)"
        required: true

env:
  YANDEX_CONTAINER_REGISTRY: ${{ secrets.YANDEX_CONTAINER_REGISTRY }}
  YANDEX_OAUTH_TOKEN: ${{ secrets.YANDEX_OAUTH_TOKEN }}

jobs:
  deploy:  
    name: Deploy Application
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to Yandex Container Registry
        run: |
          echo "$YANDEX_OAUTH_TOKEN" | docker login \
            --username oauth \
            --password-stdin \
            cr.yandex

      - name: Verify Docker image exists
        run: |
          docker pull "cr.yandex/$YANDEX_CONTAINER_REGISTRY/app:${{ inputs.RV }}_latest" || \
            { echo "Error: Docker image not found"; exit 1; }
      
      - name: Download image in VM and Deploy here
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "${{ secrets.YANDEX_OAUTH_TOKEN }}" | sudo docker login \
              --username oauth \
              --password-stdin \
              cr.yandex

            sudo docker pull cr.yandex/${{ secrets.YANDEX_CONTAINER_REGISTRY }}/app:${{ github.event.inputs.RV }}_latest
            sudo docker stop app || true
            sudo docker rm app || true
            sudo docker run -d --name app -p 3000:3000 cr.yandex/${{ secrets.YANDEX_CONTAINER_REGISTRY }}/app:${{ github.event.inputs.RV }}_latest

      - name: Find related issue
        id: find_issue
        run: |
          # Поиск issue по версии релиза
          echo "issue_title=Релиз версии ${{ inputs.RV }}" >> $GITHUB_OUTPUT

      - name: Create release comment
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // Ищем issue, содержащее версию релиза в заголовке
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'all', // Ищем среди открытых и закрытых issues
              sort: 'created',
              direction: 'desc'
            });
            
            const releaseVersion = '${{ inputs.RV }}';
            const targetIssue = issues.find(issue => 
              issue.title.includes(releaseVersion) || 
              issue.body.includes(releaseVersion)
            );

            if (!targetIssue) {
              console.log('Не найдено issue, связанного с этой версией релиза');
              return;
            }

            const commentBody = `🚀 Релиз успешно задеплоен!
            
            **Версия**: ${releaseVersion}
            **Дата**: ${new Date().toLocaleString()}
            **Выполнил**: @${context.actor}
            **Docker образ**: \`cr.yandex/${{ env.YANDEX_CONTAINER_REGISTRY }}/app:${releaseVersion}_latest\`
            
            Поздравляю с успешным релизом! 🎉`;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: targetIssue.number,
              body: commentBody
            });
